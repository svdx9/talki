pre-commit:
  commands:
    biome:
      glob: "*.{ts,tsx}"
      run: npx biome check --write {staged_files}
    backend-format:
      glob: "*.go"
      run: cd backend && make format
    backend-lint:
      glob: "*.go"
      run: cd backend && make lint
